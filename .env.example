# Server-only secrets. Do NOT prefix with REACT_APP_ (that would ship them to the browser).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: phone push when a visitor leaves an email or asks something unknown
PUSHOVER_USER=
PUSHOVER_TOKEN=
